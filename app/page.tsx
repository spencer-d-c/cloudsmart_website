import HeroSlider from '@/components/HeroSlider'

export default function Home() {
  return (
    <main>
      <HeroSlider />
      {/* Only hero slider on homepage - matching original exactly */}
    </main>
  )
}